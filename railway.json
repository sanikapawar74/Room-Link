{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && mvn clean package -DskipTests && java -Dspring.profiles.active=railway -jar target/room-link-backend-0.0.1-SNAPSHOT.jar",
    "healthcheckPath": "/api/auth/health"
  }
}
