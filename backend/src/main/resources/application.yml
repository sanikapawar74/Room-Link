server:
  port: 8080

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/puneroomie
    username: postgres
    password: koyalkam1+
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Tip: for local dev without Postgres, run with: --spring.profiles.active=h2

app:
  jwt.secret: c3VwZXJkdXBlcnNlY3JldHN1cGVyc2VjcmV0c3VwZXJzZWNyZXQ=
  jwt.expirationMs: 604800000
  cors.allowed-origins: http://localhost:5173

logging:
  level:
    org.springframework.security: INFO
